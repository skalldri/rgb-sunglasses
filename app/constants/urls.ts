// External URLs referenced by the app.

// Published privacy policy (GitHub Pages, served from the repo's `site/` folder —
// see .github/workflows/pages.yml). Linked from the Connect tab footer to satisfy
// Google Play's requirement that the policy be reachable from inside the app.
export const PRIVACY_POLICY_URL = 'https://skalldri.github.io/rgb-sunglasses/';
