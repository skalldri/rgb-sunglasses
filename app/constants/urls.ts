// External URLs referenced by the app.

// Published privacy policy on the project site (GitHub Pages, custom domain
// rgb-sunglasses.autom8ed.com — served from the repo's `site/` folder; see
// .github/workflows/pages.yml and site/CNAME). Linked from the Connect tab
// footer to satisfy Google Play's requirement that the policy be reachable
// from inside the app.
export const PRIVACY_POLICY_URL = 'https://rgb-sunglasses.autom8ed.com/privacy';
