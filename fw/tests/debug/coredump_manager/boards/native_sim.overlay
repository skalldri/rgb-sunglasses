/*
 * RAM disk for coredump_manager filesystem tests.
 * Mounted at /NAND: to match the firmware's real dump directory prefix.
 */
/ {
	nand_ramdisk: ramdisk0 {
		compatible = "zephyr,ram-disk";
		disk-name = "NAND";
		sector-size = <512>;
		sector-count = <256>;
	};
};
