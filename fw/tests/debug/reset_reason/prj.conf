CONFIG_ZTEST=y
CONFIG_REQUIRES_FULL_LIBC=y

# reset_reason_core.c needs only the RESET_* constants from zephyr/drivers/hwinfo.h,
# which are plain macros — no driver, no hardware, so HWINFO itself stays off.
