#
# Copyright (c) 2022 Nordic Semiconductor
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#

CONFIG_MAIN_STACK_SIZE=10240
# Flash
CONFIG_FLASH=y

# MCUBoot serial
CONFIG_MCUBOOT_SERIAL=y
CONFIG_MCUBOOT_SERIAL_DIRECT_IMAGE_UPLOAD=y

# CHOICE: Serial Device
#CONFIG_BOOT_SERIAL_UART=y
CONFIG_BOOT_SERIAL_CDC_ACM=y
# ENDCHOICE: Serial Device

# MCUBoot USB Serial device
#CONFIG_BOOT_SERIAL_CDC_ACM=y

# Decrease memory footprint
CONFIG_CBPRINTF_NANO=y
CONFIG_CONSOLE=n
CONFIG_CONSOLE_HANDLER=n
CONFIG_UART_CONSOLE=n
CONFIG_RTT_CONSOLE=n
CONFIG_USE_SEGGER_RTT=n
CONFIG_LOG=n

# Make MCUBoot as small as possible
CONFIG_SIZE_OPTIMIZATIONS=y

# The following configurations are required to support serial recovery of the
# network image
CONFIG_PCD_APP=y

CONFIG_FLASH_SIMULATOR=y
CONFIG_FLASH_SIMULATOR_DOUBLE_WRITES=y
CONFIG_FLASH_SIMULATOR_STATS=n

CONFIG_BOOT_IMAGE_ACCESS_HOOKS=y
CONFIG_NRF53_RECOVERY_NETWORK_CORE=y

# Devkit doesn't have an LED to indicate :(
# CONFIG_MCUBOOT_INDICATION_LED=y

CONFIG_OUTPUT_PRINT_MEMORY_USAGE=y

# Set the size of the MCUBoot partition to 64k
CONFIG_PM_PARTITION_SIZE_MCUBOOT=0x10000

# Disable MCUboot downgrade prevention to allow developers to flash
# dev builds with lower version numbers than previous builds.
# Note: Network core is already exempted from this check in MCUboot.
CONFIG_MCUBOOT_DOWNGRADE_PREVENTION=n

# Share MCUboot's own version with the app via retained SRAM (blinfo TLV).
# The app reads it via blinfo_lookup(BLINFO_BOOTLOADER_VERSION).
CONFIG_BOOT_SHARE_DATA=y
CONFIG_BOOT_SHARE_DATA_BOOTINFO=y
CONFIG_BOOT_SHARE_BACKEND_RETENTION=y
CONFIG_RETAINED_MEM=y
CONFIG_RETENTION=y