CONFIG_HEAP_MEM_POOL_SIZE=8192
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048

CONFIG_MBOX=y
CONFIG_IPC_SERVICE=y

CONFIG_BT=y
CONFIG_BT_HCI_RAW=y
CONFIG_BT_CTLR_ASSERT_HANDLER=y
CONFIG_BT_MAX_CONN=16

# Enable and adjust the below value as necessary
# CONFIG_BT_BUF_EVT_RX_COUNT=16
# CONFIG_BT_BUF_EVT_RX_SIZE=255
# CONFIG_BT_BUF_ACL_RX_SIZE=255
# CONFIG_BT_BUF_ACL_TX_SIZE=251
# CONFIG_BT_BUF_CMD_TX_SIZE=255

# Large Bluetooth data packets (251-byte DLE, speeds up DFU over Bluetooth):
# CONFIG_BT_CTLR_DATA_LENGTH_MAX is deliberately NOT set — it belongs to
# Zephyr's split controller (BT_LL_SW_SPLIT), which this image doesn't use
# (the SoftDevice Controller, BT_LL_SOFTDEVICE, is the default). Assigning it
# here never took effect and just produced a Kconfig warning (issue #164).
# With the SDC, supported data length follows the HCI buffer sizes — the
# commented BT_BUF_* block above is the knob if DFU throughput needs tuning.

CONFIG_ASSERT=y
CONFIG_DEBUG_INFO=y

CONFIG_IPC_RADIO_BT=y
CONFIG_IPC_RADIO_BT_HCI_IPC=y

# Enable net core version reporting via IPC
CONFIG_NETCORE_VERSION_IPC=y
# TinyCrypt is deprecated upstream (its "Deprecated symbol" build warning is
# accepted, see fw/CLAUDE.md "Known non-blocking build warnings"). It is still
# the SHA-256 used by fw/ipc_radio/src/netcore_version.c to hash the running
# netcore image; migrating that to PSA/mbedTLS on this flash-tight core is
# tracked as a follow-up to issue #164.
CONFIG_TINYCRYPT=y
CONFIG_TINYCRYPT_SHA256=y
