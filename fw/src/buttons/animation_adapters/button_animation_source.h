#pragma once

#include <animations/animation_button_source.h>

/** @brief Returns the single, always-registered AnimationButtonSource for this firmware. */
AnimationButtonSource &button_animation_source();
